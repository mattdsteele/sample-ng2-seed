import Hello from './hello';

describe('a js thing', () => {
  it('does a thing', () => {
    const h = new Hello();
    const x = 1;
  });
  it('does a thing', () => {
    const x = 1;
  });
  it('does a thing', () => {
    const x = 1;
  });
  it('does a thing', () => {
    const x = 1;
  });
});
